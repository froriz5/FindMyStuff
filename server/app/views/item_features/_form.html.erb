<%= form_for(@item_feature) do |f| %>
  <% if @item_feature.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item_feature.errors.count, "error") %> prohibited this item_feature from being saved:</h2>

      <ul>
      <% @item_feature.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :hidden %><br />
    <%= f.check_box :hidden %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
